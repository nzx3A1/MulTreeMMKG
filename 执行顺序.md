## 📋 项目阶段划分总览

| 阶段 | 名称 | 核心产出 | 依赖 |
|------|------|----------|------|
| **阶段一** | 基础设施层 | 数据模型 + 配置 + 工具类 | 无 |
| **阶段二** | 解析与骨架层 | MinerU解析 + 章节树 + 文档骨架 + Chunk | 阶段一 |
| **阶段三** | Schema与摘要层 | 领域Schema + 摘要生成 + 抽取基类 | 阶段一 |
| **阶段四** | 多模态抽取层 | 文本/表格/图片/公式四路抽取 | 阶段二、三 |
| **阶段五** | 对齐与发现层 | 跨模态对齐去重 + 关系发现 | 阶段四 |
| **阶段六** | 图谱与持久化层 | 图谱融合 + 校验 + Neo4j写入 | 阶段五 |
| **阶段七** | 流水线与测试层 | 主入口 + 测试 + 完整跑通 | 阶段六 |

---

## 🔧 各阶段详细任务

### 阶段一：基础设施层（3个任务）

**目标**：搭建所有下游模块依赖的基础底座。

| 子任务 | 涉及文件 | 说明 |
|--------|----------|------|
| **1.1 数据模型层** | `model/` 下所有文件 | 用 Pydantic 实现 `MMKGBaseModel`、`Document/Section/Chunk`、`Entity/Relation/Event`、`Graph`、`PipelineStage` 等所有数据模型 |
| **1.2 配置系统** | `config/app_config.py`、`config/model_config.py`、`config/neo4j_config.py`、`config/schema_config.py` | 实现 `settings` 单例，支持从 `.env` 读取密钥，定义所有配置项 |
| **1.3 工具类** | `src/utils/llm_client.py`、`vlm_client.py`、`embedding_client.py`、`json_io.py`、`logger.py` | 实现 LLM/VLM/Embedding 客户端（OpenAI 兼容）、JSON 读写工具、日志工具 |

**阶段一完成标志**：`tests/` 下能跑通基础工具类的冒烟测试。

---

### 阶段二：解析与骨架层（4个任务）

**目标**：从 MinerU 输出到构建完整的文档结构骨架。

| 子任务 | 涉及文件 | 说明 |
|--------|----------|------|
| **2.1 MinerU 加载器** | `src/parser/mineru_loader.py` | 读取 `data/mineru_output/` 下的 `full.md`、`content_list.json`、`layout.json`，归一化为内部 `Paper` 结构 |
| **2.2 Markdown 章节解析** | `src/parser/markdown_parser.py`、`src/parser/section_splitter.py` | 根据标题层级、编号规则解析 Markdown，生成章节树结构（`Document → Section → SubSection`） |
| **2.3 文档骨架图构建** | `src/skeleton/document_skeleton_builder.py` | 生成论文目录骨架图（含 `Document/Section/SubSection/Chunk/Figure/Table/Formula` 节点和 `HAS_SECTION/NEXT` 等关系） |
| **2.4 多模态 Chunk 构建** | `src/skeleton/chunk_builder.py` | 按模态类型（text/table/image/formula）划分内容块，每个 Chunk 绑定来源章节、页码、顺序号 |

**阶段二完成标志**：能生成 `stage_01` 到 `stage_05` 的 JSON 输出。

---

### 阶段三：Schema 与摘要层（3个任务）

**目标**：定义领域知识边界，实现摘要和抽取的基础能力。

| 子任务 | 涉及文件 | 说明 |
|--------|----------|------|
| **3.1 石油地质领域 Schema** | `schema/entity_schema.json`、`schema/relation_schema.json`、`schema/event_schema.json` | 按 README 中推荐的实体/关系白名单，定义完整的 Schema（含类型、属性、约束） |
| **3.2 自底向上摘要生成器** | `src/summarizer/bottom_up_summarizer.py` | 实现 Chunk → 子章节 → 章节 → 论文的自底向上摘要，可选启用 |
| **3.3 Schema 约束抽取基类** | `src/extractors/schema_constrained_extractor.py` + `prompts/` 基础模板 | 封装统一的抽取逻辑：注入 Schema → 调用 LLM → 解析 JSON → jsonschema 校验 → 返回结构化结果 |

**阶段三完成标志**：摘要模块能独立运行，抽取基类能通过 Schema 约束 LLM 输出。

---

### 阶段四：多模态抽取层（4个任务）

**目标**：分别从文本、表格、图片、公式中抽取实体和关系。

| 子任务 | 涉及文件 | 说明 |
|--------|----------|------|
| **4.1 文本抽取器** | `src/extractors/text_extractor.py` + `prompts/text_extraction_prompt.md` | 从正文 Chunk 中抽取石油地质实体、属性、关系和事件 |
| **4.2 表格抽取器** | `src/extractors/table_extractor.py` + `prompts/table_extraction_prompt.md` | 从 Markdown 表格中抽取参数、样品、实验数据等结构化信息 |
| **4.3 图片抽取器** | `src/extractors/image_extractor.py` + `prompts/image_extraction_prompt.md` | 调用 VLM 从地质图、剖面图、岩心照片等中抽取视觉实体和关系 |
| **4.4 公式抽取器** | `src/extractors/formula_extractor.py` + `prompts/formula_extraction_prompt.md` | 从 LaTeX 公式中抽取符号、参数、模型和计算关系 |

**阶段四完成标志**：能生成 `stage_06` 到 `stage_09` 的四个抽取结果 JSON。

---

### 阶段五：对齐与发现层（3个任务）

**目标**：解决跨模态重复问题，发现潜在关系。

| 子任务 | 涉及文件 | 说明 |
|--------|----------|------|
| **5.1 实体对齐与去重** | `src/alignment/entity_aligner.py`、`src/alignment/deduplicator.py` | 基于名称相似度、Embedding 语义、类型一致性、LLM 判定，对不同模态的实体进行聚类合并 |
| **5.2 关系对齐与去重** | `src/alignment/relation_aligner.py` | 基于两端实体对齐结果、关系类型归一化、证据来源去重，合并重复关系 |
| **5.3 关系发现** | `src/discovery/relation_discovery.py` + `prompts/relation_discovery_prompt.md` | 在已对齐实体之上，由 LLM 挖掘跨章节、跨模态的潜在新关系（如 `supports`、`quantifies`） |

**阶段五完成标志**：能生成 `stage_10`（对齐去重）和 `stage_11`（关系发现）的 JSON。

---

### 阶段六：图谱与持久化层（3个任务）

**目标**：融合所有阶段数据，校验后写入 Neo4j。

| 子任务 | 涉及文件 | 说明 |
|--------|----------|------|
| **6.1 图谱融合器** | `src/graph/graph_merger.py` | 合并骨架图、抽取结果、对齐结果、发现结果，形成统一增强图谱，附溯源边 |
| **6.2 图谱校验器** | `src/graph/graph_validator.py`、`src/graph/graph_schema.py` | 校验节点引用完整性、Schema 一致性、必填字段完整性、置信度范围 |
| **6.3 Neo4j 写入器** | `src/graph/neo4j_writer.py` + `schema/schema_graph.cypher` | 创建约束索引，批量 MERGE 写入节点和关系，支持按文档增量写入 |

**阶段六完成标志**：能生成 `stage_12_enhanced_graph.json` 和 `final_graph.json`，并成功写入 Neo4j。

---

### 阶段七：流水线与测试层（3个任务）

**目标**：串起完整流水线，补全测试，确保端到端跑通。

| 子任务 | 涉及文件 | 说明 |
|--------|----------|------|
| **7.1 流水线主入口** | `run_pipeline.py` + `model/pipeline.py` | 实现命令行参数解析、阶段调度、断点续跑（`--from-stage`/`--to-stage`）、错误处理 |
| **7.2 Prompt 模板调优** | `prompts/` 下所有文件 | 根据实际运行效果优化各阶段 Prompt，提升抽取准确率和稳定性 |
| **7.3 单元测试补全** | `tests/` 下所有文件 | 补全 parser、extractor、alignment、neo4j_writer 的单元测试，确保核心逻辑覆盖率 |

**阶段七完成标志**：`python run_pipeline.py --input data/raw_pdf/paper_001.pdf` 能完整跑完所有阶段，Neo4j 中可见图谱。

---

## 💡 执行建议

1. **按顺序执行**：阶段之间有依赖关系，建议严格按一→七顺序推进
2. **每个子任务独立交付**：每个子任务完成后，用对应的 `tests/` 或手动验证后再进入下一个
3. **阶段三可前置**：Schema 定义（3.1）可以和阶段二并行启动，因为它不依赖解析代码
4. **阶段一最关键**：数据模型和工具类是所有后续工作的基础，务必先把这层打扎实
5. **善用阶段持久化**：每个阶段输出的 JSON 都是天然的验收标准，可以用来验证当前阶段是否正确

需要我帮你从**阶段一**开始具体实现吗？